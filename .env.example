# Server Configuration
PORT=3001

# Database Configuration
DATABASE_URL=postgres://username:password@hostname:5432/database_name

# In production on DigitalOcean, the DATABASE_URL will be provided as an environment variable
# For local development, you can use a local PostgreSQL instance or connect to a remote database

# Node Environment
NODE_ENV=development